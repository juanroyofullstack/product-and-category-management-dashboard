const RowContainer = () => { 
    return (
        <div className="flex flex-col items-center justify-center w-full h-full gap-4">
            {/* This is where the rows will be rendered */}
        </div>
    );
};

export default RowContainer;